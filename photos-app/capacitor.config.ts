import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photproject.app',
  appName: 'photos-app',
  webDir: 'dist/photos-app',
  bundledWebRuntime: false
};

export default config;
